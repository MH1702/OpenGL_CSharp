namespace OpenGL_CS.Enums {
	public enum ObjectIdentifier : uint {
		Buffer = (uint)ALL.GL_BUFFER,
		Shader = (uint)ALL.GL_SHADER,
		Program = (uint)ALL.GL_PROGRAM,
		Vertex_Array = (uint)ALL.GL_VERTEX_ARRAY,
		Query = (uint)ALL.GL_QUERY,
		Program_Pipeline = (uint)ALL.GL_PROGRAM_PIPELINE,
		Transform_Feedback = (uint)ALL.GL_TRANSFORM_FEEDBACK,
		Sampler = (uint)ALL.GL_SAMPLER,
		Texture = (uint)ALL.GL_TEXTURE,
		Renderbuffer = (uint)ALL.GL_RENDERBUFFER,
		Framebuffer = (uint)ALL.GL_FRAMEBUFFER,
	}
}
